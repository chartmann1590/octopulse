# dependencies
node_modules/
package-lock.json

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# local env files - NEVER commit real AdMob IDs / secrets
.env
.env.local
.env.*.local
.env.development.local
.env.production.local
.env.production
*.env
# allow template (contains no secrets)
!.env.example

# secrets - NEVER commit (AdMob IDs are in .env / GitHub Secrets)
google-services.json
GoogleService-Info.plist
firebase.json
.firebaserc
eas.json
secrets/
*keystore*
*service-account*.json
# AdMob production IDs must NOT be hardcoded - use env vars
# See .env.example and app.config.js

# typescript
*.tsbuildinfo

# Android build artifacts
android/.gradle/
android/app/build/
android/build/
android/.cxx/
android/app/.cxx/
android/local.properties
android/*.iml
android/app/*.iml

# iOS
/ios

# logs
logs/
*.log

# coverage
coverage/

# promo build artifacts (keep docs/video/promo.mp4 but ignore tmp)
tools/tmp_audio/
tools/tmp_clips/
tools/tmp_slides/
tools/site-preview.png
tools/promo-section.png
tools/screenshots-section.png
